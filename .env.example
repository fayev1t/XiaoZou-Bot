# 应用运行配置
LOG_LEVEL=DEBUG
ENVIRONMENT=dev
DRIVER=~fastapi
LOCALSTORE_USE_CWD=true
ONEBOT_ACCESS_TOKEN=your_onebot_access_token
HOST=0.0.0.0
PORT=7500
SUPERUSERS=["123456789"]
NICKNAME=["小奏"]
COMMAND_START=["/", ""]
COMMAND_SEP=["."]
QQBOT_ENABLE_TEST_EVENTS=0

# 应用数据库：双机部署时，应用服务器直接访问 Docker 服务服务器上的 PostgreSQL
DATABASE_URL=postgresql+asyncpg://admin:your_postgres_password@your_docker_service_host:7504/mydb

# LLM 配置
LLM_PROVIDER=openai
LLM_API_KEY=your_llm_api_key
LLM_MODEL=gpt-5.4
LLM_TEMPERATURE=0.7
LLM_BASE_URL=https://xxxx/v1
LLM_MAX_TOKENS=500

# NapCat 重要配置（当前 docker/napcat/compose.yml 对照值）
NAPCAT_WEBUI_HOST_PORT=7501
NAPCAT_HTTP_HOST_PORT=7502
NAPCAT_WS_HOST_PORT=7503

# Postgres 重要配置（当前 docker/postgres/compose.yml 对照值）
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=mydb
POSTGRES_HOST_PORT=7504

# SearXNG 部署配置（应用侧访问 Docker 服务服务器上的 SearXNG）
SEARXNG_BASE_URL=http://your_docker_service_host:7505
SEARXNG_SECRET=change-this-searxng-secret

# Crawl4AI 部署配置（应用侧访问 Docker 服务服务器上的 Crawl4AI）
CRAWL4AI_BASE_URL=http://your_docker_service_host:7506
