# 开发日志

## 2026-03-27
- 二次调整 `README.md` 的开头简介与“功能特点”段落，不再按消息块聚合、三层判断、静默窗口等内部实现机制逐段展开，而是改成更接近项目说明的功能总结式写法，统一从群聊内容理解、上下文参与、图片解析、回复节奏、群资料同步、历史消息持久化、按群组织数据等能力角度概括项目特性，使 README 更自然也更适合作为首页介绍。
- 重写 `README.md` 的项目说明，按当前代码实现重新组织为“功能特点 / 项目实际能力 / 项目结构 / 主链路 / 技术简述 / 开发说明”几个部分，重点突出消息块聚合、分层回复决策、发送前静默窗口、图片首轮识图与按需二次识图、历史上下文刷新、群资料周期同步、按群分表与机器人回复回写等已落地能力，同时收敛对技术栈的篇幅，避免继续用过于片面或口语化的描述覆盖项目全貌。

## 2026-03-26
- 清理近期识图/聚合改动里的冗余与过时描述：移除 `message_aggregator.py` 中未使用的 `raw_message` 传递链和壳方法 `add_message()`，收敛 `group_chat.py` 中重复的图片描述刷新逻辑，修正 `prompt.py`、`README.md` 与 `开发文档/multimodal_discussion.md` 里已经落后于现实现状的多模态与分层说明，保持运行行为不变。
- 修复第一层消息块竞态与发送窗口语义：`qqbot/services/message_aggregator.py` 现在为每个群维护版本号和“入库中消息”计数，只要有新消息到达并开始入库，旧的 Layer0 判断与等待任务就会失效；只有当新消息完成入库并加入 block 后才会重新发起判断。同时在 `qqbot/plugins/group_chat.py` 中加入发送前 2 秒静默窗口，若这 2 秒内有新消息到达，则取消本次发送并继续并入新的消息块。
- 修复 OpenAI 兼容网关调用与异常日志问题：`qqbot/core/llm.py` 现在为 OpenAI 兼容客户端显式开启 `streaming=True`，以适配当前网关返回的“Stream must be set to true”要求；同时修正 `qqbot/services/message_aggregator.py` 中直接把异常字典插入 f-string 导致的二次 `KeyError` 日志问题，确保上游 400 错误能被正常记录。
- 增强识图排障信息：在 `qqbot/core/llm.py` 增加 LLM 客户端创建日志，在 `qqbot/services/image_parsing.py` 增加图片下载与视觉请求的开始/结束耗时日志，并为 `llm.ainvoke(...)` 加入 45 秒硬超时，方便区分是图片下载卡住、视觉请求卡住，还是当前 `base_url/model` 对多模态调用不兼容。
- 修正第二层回复规划语义：`qqbot/services/prompt.py` 现在明确要求先判断一个消息块是否真的包含多个需要分别回应的话题，仅在必要时才在单个 JSON 的 `replies` 数组中输出多条回复计划；每条计划代表一个独立回复任务，后续由第三层在 `qqbot/plugins/group_chat.py` 中逐条串行执行，而不是让第二层输出多个独立 JSON 语句。
- 接入图片同步首轮识图链路：启用 `ImageRecord` 图片记录表并补齐 `local_path` 字段，在 `qqbot/services/message_converter.py` 中将图片 segment 改为同步下载、哈希、落盘、写入 `image_records`、调用 `gpt-5.4` 生成首轮描述，再以 `System-Image` 标签写入 `formatted_message`；同时在 `block_judge.py` / `prompt.py` 增加 `need_image_parsing`，在 `plugins/group_chat.py` 支持按需二次识图覆盖描述，并让 `context.py` 在读取历史上下文时按最新 `ImageRecord.description` 刷新图片标签，识图失败时降级为“图片解析失败”但不阻断消息主流程。
- 统一开发环境配置来源：新增 `.env.example` 作为脱敏模板，将 `.env.dev` 收敛为开发环境权威配置，补齐 `DATABASE_URL`、`QQBOT_ENABLE_TEST_EVENTS` 等关键项，并同步收缩 `qqbot/core/database.py`、`qqbot/core/llm.py` 中分散的数据库/LLM默认值，避免配置在代码与环境文件中各自维护。
- 将默认 LLM 配置从 DeepSeek 切换为 OpenAI 兼容接口，`qqbot/core/llm.py` 默认值改为 `openai` / `gpt-5.4` / `https://api-vip.codex-for.me/v1`，并同步更新 `.env.dev` 与 `README.md` 的示例配置。
- 将当前时间写入策略收敛为 SQLite + 中国时区：新增 `qqbot/core/time.py` 统一生成 `Asia/Shanghai` 本地时间，并同步修改 `qqbot/services/group_member.py`、`user.py`、`group.py`、`group_message.py`、`message_pipeline.py`、`plugins/group_chat.py`、`services/message_converter.py`、`qqbot/models/messages.py`、`qqbot/core/database.py`、`scripts/migrate_postgres_to_sqlite.py`，不再依赖 SQLite `CURRENT_TIMESTAMP` 或服务层 UTC 时间写入。
- 精简 `.gitignore`，移除 Django / Flask / Scrapy / Jupyter / Celery 等与本项目无关的模板残留规则，最终仅保留环境文件、虚拟环境、Python 缓存、构建产物、`.ruff_cache/`、日志和 `sqlite_data/` 等实际需要忽略的内容，并去掉会误伤文档的 `*.md` 规则。
- 将 SQLite 默认数据目录从 `data/` 更名为 `sqlite_data/`，同步更新 `qqbot/core/database.py`、`scripts/migrate_postgres_to_sqlite.py`、`.env.example`、`README.md` 的默认路径示例，并在 `.gitignore`、`.dockerignore` 中新增 `sqlite_data/` 忽略规则。
- 配置加载新增 `qqbot/core/settings.py`，统一按 `.env` → `.env.<ENVIRONMENT>` 读取运行参数，不再把 `BaseSettings` 写死到 `.env.dev`。
- `pyproject.toml` 的 Python 基线统一到 3.10，并同步更新 Ruff / Pyright 的目标版本配置。
- `qqbot/__main__.py` 改为显式加载生产插件，`qqbot/plugins/test_events.py` 仅在 `QQBOT_ENABLE_TEST_EVENTS=1` 时注册调试 handler。
- `qqbot/plugins/event_handlers.py` 为群消息、进群、退群、撤回分别增加精确规则过滤，并把入群链路收敛为单次提交事务。
- 统一 `UserService` 返回契约，修复 `display_name_resolver.py`、`context.py`、`conversation.py`、`group_chat.py` 把 ORM 当 dict 访问的问题。
- `qqbot/services/user.py`、`qqbot/services/group.py` 的 `get_or_create_*` 改为幂等并发安全路径，服务层不再内部 `commit`，事务统一由调用边界处理。
- `qqbot/core/llm.py` 补充 `LLM_BASE_URL`、`LLM_MAX_TOKENS` 支持；`block_judge.py`、`conversation.py`、`message_aggregator.py` 在 LLM 不可用时改为显式降级。
- `qqbot/services/message_converter.py` 补充 System-XML 文本/属性转义，并为 reply 段增加事件回退与数据库回查逻辑，避免固定占位文本。
- `qqbot/plugins/group_chat.py` 改为按 bot `self_id` 缓存实例，拆分长生命周期数据库 session，并把机器人回复持久化放到独立短事务中。
- `qqbot/plugins/sync_nicknames.py` 改为“网络调用在 session 外、每个群独立事务”的同步方式，避免长时间持有数据库连接。
- `qqbot/plugins/startup.py` 改为注册周期任务 + 等待 bot 就绪后的首次同步，不再依赖固定 40 秒窗口或固定 bot 实例。
- `qqbot/services/message_aggregator.py`、`qqbot/services/silence_mode.py` 增加 shutdown/重置清理能力，减少长期运行后的内存状态残留。
- 恢复群消息对 OneBot `message_id` 的持久化映射：`qqbot/models/messages.py`、`qqbot/core/database.py`、`qqbot/services/group_message.py`、`qqbot/services/message_pipeline.py`、`qqbot/plugins/event_handlers.py`、`qqbot/plugins/group_chat.py` 统一新增 `onebot_message_id` 链路，用于 reply 回查与撤回标记。
- 群消息表初始化和迁移逻辑补充向后兼容：若历史表仍保留旧 `message_id` 列，会自动回填到 `onebot_message_id`；对应同步更新 `scripts/migrate_group_messages_v2.py`、`scripts/migrate_group_messages_v2.sql`、`scripts/migrate_postgres_to_sqlite.py`。
- `qqbot/plugins/event_handlers.py`、`qqbot/plugins/group_chat.py`、`qqbot/services/context.py` 改为从 `NICKNAME` 配置读取机器人显示名/别名，不再硬编码“小奏”。
- `qqbot/services/block_judge.py` 统一以 `replies` 列表长度作为回复次数真相，避免 `reply_count` 与计划列表不一致时出现“判断要回但实际不发”的静默偏差。
- `qqbot/plugins/test_events.py` 补充 `Rule(...)` 精确过滤，`qqbot/plugins/sync_nicknames.py` 的模块说明改为当前真实行为。
- 新增 `.env.example`、`CLAUDE.md`，并更新 `README.md`、`AGENTS.md`、`开发文档/开发规范.md`，使文档与当前 Python 3.10、插件加载和配置方式保持一致。
- 数据库接入层升级为支持 PostgreSQL / SQLite 双后端，`qqbot/core/database.py` 新增 `DATABASE_URL`、`DB_BACKEND`、`SQLITE_PATH` 配置解析与 SQLite 引擎适配。
- 动态分表建表逻辑兼容 SQLite，补充 SQLite 模式下的建表 DDL、WAL、busy_timeout 等连接配置。
- 修正 `qqbot/services/user.py`、`qqbot/services/group_member.py`、`qqbot/services/group_message.py` 中的 PostgreSQL 方言依赖，改为可同时兼容 SQLite 的 UPSERT / 自增 ID 处理。
- 新增迁移脚本 `scripts/migrate_postgres_to_sqlite.py`，用于将 `users`、`groups` 及各群动态成员表、消息表从 PostgreSQL 复制到 SQLite 并校验行数。
- 迁移脚本补充项目根目录自动注入 `sys.path`，支持在仓库根目录直接执行 `python scripts/migrate_postgres_to_sqlite.py`。
- 迁移文档与脚本报错提示补充宿主机连接说明：在宿主机终端执行时应连接 `127.0.0.1/localhost`，不要使用仅在 Docker 网络内可解析的容器名 `postgres16`。
- 更新 `README.md`、`CLAUDE.md`、`pyproject.toml`，补充 SQLite 配置方式、迁移命令和 `aiosqlite` 依赖说明。

## 2026-01-24
- 新增称呼返回器 `qqbot/services/display_name_resolver.py`，用于解析群成员称呼。
- 完成消息转化器 `qqbot/services/message_converter.py`，按设计要求输出 System-XML。
- 新增数据库迁移脚本 `scripts/migrate_group_messages_v2.py`（创建新表并格式化迁移历史数据）。
- 更新 `scripts/migrate_group_messages_v2.sql` 仅创建表结构（数据迁移由 Python 脚本完成）。
- 补充 `qqbot/services/group_message.py` 的 message_id 查询方法。
- 全量重构数据服务为实例化用法，统一围绕 v2 消息表结构。
- 新增消息处理管线 `qqbot/services/message_pipeline.py`，事件入口改为统一转换并存储。
- 聚合与上下文链路改用 `formatted_message`，配合 System-XML 输入。

## 2026-01-25
- 上下文读取在 `formatted_message` 为空时回退到 `raw_message`，避免旧数据缺失内容。
- 回复引用内容在无 `formatted_message` 时回退到 `raw_message`，保证历史消息可读。
- 新增好友请求自动通过与私聊引导回复处理器 `qqbot/plugins/friend_private.py`。
- 群消息只转换一次：入库后由 `event_handlers.py` 直接送入聚合器，`group_chat.py` 不再重复生成。
- 在提示词中补充 System-XML 标签语义，帮助模型明确理解消息结构。
- System-Message 增加 user_id/display_name/timestamp 元信息，并同步迁移脚本与机器人回复包装。
- 对话块与历史上下文直接使用 System-Message 输出，取消额外的 QQ号/时间前缀拼接。
- 移除 System-Reply 的 reply_message_id 属性，并删除群消息表中的 message_id 列。
- 新增移除列脚本 `scripts/remove_message_id_column.sql`。
- 移除列脚本同步处理 `group_messages_v2_template` 与 `group_messages_template`。
- 更新 `开发文档/数据库设计.md` 以匹配 v2 消息表结构。
- 精简提示词内容，保留关键规则与 System-XML 语义说明。
- System-Message 移除 message_id 属性，仅保留 user_id/display_name/timestamp。
- 私聊与好友请求处理器增加规则过滤，避免拦截群消息事件。
- 处理群消息时优先使用 `original_message` 生成 `raw_message` 与 System-XML，避免 NoneBot 识别昵称后吞掉前缀文本。
- 回复生成提示词补充禁止输出 System-XML/CQ 标签，`conversation.py` 增加 System-XML 标签清理。
- 修复 `friend_private` 规则函数参数类型，避免 Rule 解析报错导致插件加载失败。

## 2026-03-01 架构优化与代码清理
1. **统一引入 loguru**：全局移除标准库 `logging` 和所有 `print()`，使用 `loguru` 提供带颜色的控制台输出及按日轮转的日志文件，并新增 AI I/O 专用日志流和错误日志记录。
2. **System-XML 格式转换解耦**：引入 `MessageConverter` 负责处理 OneBot 格式的解构与转换，增加 `MessagePipeline` 进行消息保存统筹，重构 `ContextManager` 以统一返回格式。
3. **LLM 工厂化**：修正三层 AI 各自独立实例化 `ChatOpenAI` 并硬编码 `base_url` 的问题。统一在 `core/llm.py` 通过 `create_llm` 获取，确保复用配置文件与连接池。
4. **单例模式及生命周期控制**：修复 `ConversationService` 每次生成回复都会创建新实例（导致缓存失效）的问题，将其升级为全局单例。清理 `startup.py` 内同步昵称任务的重复注册。
5. **废弃特性清理与安全加固**：
   - 将 `datetime.utcnow()` 替换为带时区的 `datetime.now(timezone.utc)`。
   - 去除测试用插件 `test_events.py` 中向线上群聊直接发回执的安全隐患。
   - 修正 `core/database.py` 中连接 URL 打印直接泄露密码的风险。
   - 调整类型注解规范，如废除 `typing.Dict` 使用原生的 `dict` 等。
6. **分表与依赖解耦**：将所有 Service 类（如 `GroupService`，`UserService`）从纯静态方法重构为依赖注入 `session` 的实例方法。群消息模型字段重构为 `raw_message` 与 `formatted_message`，并自动为每个群建立新的分表 `group_messages_v2_{id}`。
